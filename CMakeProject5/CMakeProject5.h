// CMakeProject5.h : Include file for standard system include files,
// or project specific include files.

typedef void V;
typedef int I;
typedef char C;
typedef long L;
typedef float E;
typedef short S;

#define R return
#define O printf
#define Od(x) printf("%ld\t[%ld]\n",x,t2d(x))
#define Oc(x) printf("%c",A[x])
#define EXIT {printf("!MEM\n");exit(4);}

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <wchar.h>
#include <locale.h>

L *M;
S *B;
C *A;
L *R0,*R1,*R2,*R3;

// TODO: Reference additional headers your program requires here.
